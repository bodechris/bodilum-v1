import type { MetadataRoute } from "next";
export default function sitemap(): MetadataRoute.Sitemap { const base = "https://tools.bodilum.com"; return ["", "/prospect-finder", "/brand-scorecard", "/privacy", "/terms", "/acceptable-use"].map((path) => ({ url: `${base}${path}`, lastModified: new Date(), changeFrequency: path === "" ? "weekly" : "monthly", priority: path === "" ? 1 : (path === "/prospect-finder" || path === "/brand-scorecard") ? 0.9 : 0.3 })); }
